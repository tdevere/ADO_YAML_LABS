# Track only .yml files in this repository
# Ignore everything by default
*

# Don't ignore .gitignore itself
!.gitignore

# Allow YAML examples with .yml extension anywhere in the repo
!*.yml
!**/*.yml
